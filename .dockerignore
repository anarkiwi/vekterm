# Keep the build context small. NOTE: third_party/libpitrex is vendored and must
# be sent to the build, so do not exclude it (or *.a / *.o globally).
.git
.github
.claude

# Build outputs (rebuilt inside the image)
build.baremetal/
out/
/vekterm
/kernel.img
/vekterm.img
/frame.bin
/tests/test_protocol
/tests/test_frame
/tests/test_coord

# Local cruft
.DS_Store
*.swp
*~
.venv/
__pycache__/
