# Build outputs (note: third_party/libpitrex/*.a are vendored and tracked)
build.baremetal/
/vekterm
/kernel.img
/kernel7.img
/vekterm.img
/out/
/out-*/
/frame.bin

# Compiled unit tests (the .c sources are tracked)
/tests/test_protocol
/tests/test_frame
/tests/test_coord
/tests/test_parser

# OS / editor cruft
.DS_Store
*.swp
*~
*.dSYM/

# Python (CI lint tooling, if a venv is created locally)
.venv/
__pycache__/
